import 'package:flutter/material.dart';

class DesignerSettings extends StatelessWidget {
  const DesignerSettings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
